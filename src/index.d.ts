/**
 * Return the daily wordle answer
 * @param dateString 
 * Optional date string of format 'yyyy-MM-dd' to get word for. Defaults to today
 * @returns
 * The daily wordle answer
 * @throws
 * If the supplied string is defined and not of the proper format
 * @throws
 * If date is before the earliest possible date
 * @throws
 * If date is after the latest possible date
 */
export const getAnswer = (dateString?: string) => string
